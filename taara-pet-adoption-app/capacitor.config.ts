import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taara.petadoption',
  appName: 'Taara Pet Adoption',
  webDir: 'build'
};

export default config;
